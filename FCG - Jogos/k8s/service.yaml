apiVersion: v1
kind: Service
metadata:
  name: jogos-svc
  namespace: fiapk8s
  labels:
    app: jogos-api
spec:
  type: LoadBalancer
  selector:
    app: jogos-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
