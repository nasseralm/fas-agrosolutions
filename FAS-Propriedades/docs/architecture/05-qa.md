# 05 — QA (build/test) + ajustes para qualidade

## Comandos executados (Release)

### FAS — Identity

- `cd FAS-Usuarios`
- `dotnet restore FAS.sln`
- `dotnet build FAS.sln -c Release --no-restore`
- `dotnet test FAS.sln -c Release --no-build`
- Resultado: **build OK**, **16 testes OK**

### FAS — Properties (Propriedades/Talhões)

- `cd FAS-Propriedades`
- `dotnet restore FAS.sln`
- `dotnet build FAS.sln -c Release --no-restore`
- `dotnet test FAS.Tests/FAS.Tests.csproj -c Release`
- Resultado: **build OK**, **9 testes OK**

### FCG — Usuários

- `cd "FCG - Usuarios"`
- `dotnet restore FCG.sln`
- `dotnet build FCG.sln -c Release --no-restore`
- `dotnet test FCG.sln -c Release --no-build`
- Resultado: **build OK**, **7 testes OK**

### FCG — Pagamentos

- `cd "FCG - Pagamentos"`
- `dotnet restore/build/test FCG.sln -c Release`
- Resultado: **build OK**
- Observação: `dotnet test` reporta **“não há testes disponíveis”** no assembly `FCG.Tests` (não falha o pipeline no estado atual).

### FCG — Jogos

- `cd "FCG - Jogos"`
- `dotnet restore/build/test FCG.sln -c Release`
- Resultado: **build OK**
- Observação: `dotnet test` reporta **“não há testes disponíveis”** no assembly `FCG.Tests` (não falha o pipeline no estado atual).

## Analyzers / lint / format

- Não foi encontrado (neste workspace) um pipeline de lint/format dedicado (ex.: `dotnet format`, StyleCop, etc.) além dos analyzers padrão do SDK.

## Ajustes feitos para “build limpo”

- Rename de diretórios FCG:
  - `FCG%20-%20Usuarios` → `FCG - Usuarios`
  - `FCG%20-%20Pagamentos` → `FCG - Pagamentos`
  - `FCG%20-%20Jogos` → `FCG - Jogos`
  - Motivo: o `dotnet` decodificava `%20` e quebrava `restore/build` por path inexistente.
- Correção do Dockerfile do `FAS-Usuarios`:
  - `FAS-Usuarios/Dockerfile`: corrigido `dotnet publish "FCG.API.csproj"` → `dotnet publish "FAS.API.csproj"`.

## Evidência resumida

- Todos os `.sln` do workspace **compilam em Release sem erro**.
- Testes existentes passam:
  - `FAS-Usuarios`: 16
  - `FAS-Propriedades`: 9
  - `FCG - Usuarios`: 7

